import sys
from ..skimage.filters import *

# Add the dynamically generated functions to the current module namespace
# current_module = sys.modules[__name__]

# for name, func in plugin_functions.items():
#     setattr(current_module, name, func)